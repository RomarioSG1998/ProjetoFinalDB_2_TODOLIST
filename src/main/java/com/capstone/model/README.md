# Model Layer

Este diretório contém as **Entidades** da aplicação.

### Responsabilidades:
- Mapear as tabelas do banco de dados para classes Java usando JPA (@Entity).
- Definir os campos, tipos e restrições (ex: tamanho de colunas, campos obrigatórios).
- Representar o estado e o domínio da aplicação.

# Controller Layer

Este diretório contém os **REST Controllers** da aplicação.

### Responsabilidades:
- Expor os endpoints da API (rotas HTTP).
- Validar as entradas de dados básicas.
- Delegar o processamento para a Camada de **Service**.
- Retornar as respostas HTTP adequadas (`ResponseEntity`) para o cliente.
